.DS_Store
.build/
dist/
*.mov
*.mp4

# The Sparkle signing key. Whoever has it can publish an update this app
# will install without asking; it belongs in a password manager, not here.
sparkle-private-key.txt
*private-key*
